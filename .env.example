# Frontend (Vercel) — URL do servidor de áudio em produção
VITE_API_URL=https://seu-backend.onrender.com

# Backend (Render/Railway) — domínios permitidos (separados por vírgula)
ALLOWED_ORIGINS=https://seu-app.vercel.app,http://localhost:5173

# Backend (opcional) — cookies do YouTube em formato Netscape, se houver bloqueio
# YOUTUBE_COOKIES=# Netscape HTTP Cookie File...
